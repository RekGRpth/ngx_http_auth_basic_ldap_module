name: Merge
on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:
jobs:
  merge:
    runs-on: ubuntu-latest
    steps:
      - uses: rekgrpth/merge-upstream@v1
        with:
          upstream: godvsdeity/ngx_http_auth_basic_ldap_module
